# Testing dependencies for davinci-doc-creator backend
# Install with: pip install -r requirements-test.txt

# Core dependencies (from requirements.txt)
-r requirements.txt

# Testing frameworks
pytest==7.4.3
pytest-cov==4.1.0

# PDF testing
PyPDF2==3.0.1
